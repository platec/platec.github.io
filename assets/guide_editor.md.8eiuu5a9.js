import{_ as e,o as a,c as d,am as s}from"./chunks/framework.D1jVa83F.js";const g=JSON.parse('{"title":"图形编辑器","description":"","frontmatter":{},"headers":[],"relativePath":"guide/editor.md","filePath":"guide/editor.md"}'),o={name:"guide/editor.md"};function i(r,t,n,l,c,p){return a(),d("div",null,[...t[0]||(t[0]=[s(`<h1 id="图形编辑器" tabindex="-1">图形编辑器 <a class="header-anchor" href="#图形编辑器" aria-label="Permalink to &quot;图形编辑器&quot;">​</a></h1><p><strong>Leaf Plus Editor</strong> 是基于 <code>@leaf-plus/core</code> 构建的完整图形编辑器（Vue 3 + naive-ui），覆盖<strong>图纸 / 图标（矢量符号）/ 组件</strong>三类资源的编辑。它既是开箱即用的作图工具，也是核心库全部能力的最佳实践参考。</p><div class="tip custom-block"><p class="custom-block-title">在线体验</p><p>👉 <strong><a href="https://leafplus.me" target="_blank" rel="noreferrer">https://leafplus.me</a></strong> —— 在线访问，打开即用，无需安装。</p></div><h2 id="功能概览" tabindex="-1">功能概览 <a class="header-anchor" href="#功能概览" aria-label="Permalink to &quot;功能概览&quot;">​</a></h2><table tabindex="0"><thead><tr><th>模块</th><th>能力</th></tr></thead><tbody><tr><td><strong>资源管理</strong></td><td>图纸 / 图标 / 资源 / 组件四个页签；新建、重命名、删除、树形分组；支持导入 <strong>Mermaid</strong>、<strong>DXF</strong>、<strong>SVG</strong>、<strong>drawio</strong> 文件生成图纸</td></tr><tr><td><strong>绘制工具</strong></td><td>选择、矩形、圆角矩形、圆形、三角形、多种多边形、文本、<strong>富文本</strong>、折线、画笔（自由线条）、连接线、动画</td></tr><tr><td><strong>属性面板</strong></td><td>属性 / 样式 / 绑定三个页签：位置尺寸、旋转锚点、全部样式键、图层归属、标签位置选择器等；<strong>滤镜编辑器</strong>支持多滤镜链与参数化编辑（35 种内置效果，旧版滤镜自动提示迁移）；<strong>动画编辑器</strong>基于效果元信息生成参数表单，并支持 <code>$play</code> / <code>$speed</code> 及动画参数的属性绑定；折线 / 连接线支持<strong>控制点模式编辑</strong>（line / mirror / disconnected / asymmetric）；样式页签含「布局」组：宿主图元（可从图形列表拖入）、全屏适配、水平 / 垂直布局约束</td></tr><tr><td><strong>图标编辑</strong></td><td>矢量符号的绑定清单（<code>bind[]</code>）配置、样式关联绑定对话框、吸附锚点（AttachPoint）编辑、组件槽位配置（见 <a href="./binding.html">图标绑定</a>）。组件既可拖入<strong>图标画布</strong>，也可直接拖入<strong>图纸</strong>使用；选中后属性检查器展示其配置项（组件 <code>properties</code>）。组件定义在内存草稿中编辑、保存即刷新全部画布缓存</td></tr><tr><td><strong>画布交互</strong></td><td>框选 / 多选、拖拽移动、8 手柄缩放、旋转、控制点编辑、右键菜单（触屏长按唤起）、资源触屏拖拽入画布、元素列表；复制 / 粘贴保留旋转缩放、重映射 id 并重写连接线引用；图纸属性可配置「运行交互」（适配内容 / 可缩放 / 可平移）</td></tr><tr><td><strong>历史记录</strong></td><td>撤销（Ctrl+Z）/ 重做（Ctrl+Y），交互操作自动合并为单条历史</td></tr><tr><td><strong>导出</strong></td><td>导出 PNG 图片、导出 SVG（可编辑矢量，图标导出为透明背景）、导出 JSON（<code>storage.serialize()</code>）</td></tr><tr><td><strong>预览</strong></td><td>以非编辑模式打开图纸 / 图标，实时查看动画、物理模拟与交互效果</td></tr></tbody></table><h2 id="图形库-stencils" tabindex="-1">图形库（Stencils） <a class="header-anchor" href="#图形库-stencils" aria-label="Permalink to &quot;图形库（Stencils）&quot;">​</a></h2><p>编辑器内置 mxGraph / drawio 图形库（AWS、Azure、BPMN、Cisco、阿里云等数十个类目），按需懒加载本地 XML 库文件。拖入画布的形状即核心库的 <code>Shape</code>（<code>shape: &#39;stencil:&lt;name&gt;&#39;</code> 或 <code>&#39;mxgraph.*&#39;</code>），绘制与序列化均由核心库完成。</p><h2 id="自动布局" tabindex="-1">自动布局 <a class="header-anchor" href="#自动布局" aria-label="Permalink to &quot;自动布局&quot;">​</a></h2><p>工具栏 / 属性面板的布局对话框直接调用核心库布局插件的 <code>view.layout(options)</code>，对画布上的「节点 + 连接线」图做自动排布：</p><table tabindex="0"><thead><tr><th>布局</th><th>说明</th></tr></thead><tbody><tr><td>脑图（mindmap）</td><td>中心向外发散，支持 5 种方向</td></tr><tr><td>树形（tree）</td><td>层级树，父节点居中于子节点</td></tr><tr><td>环形（circular）</td><td>中心辐射 / 等分成环</td></tr><tr><td>力导向（force）</td><td>自然铺开的网络图；可写入物理样式，预览时可拖拽回弹</td></tr><tr><td>分层（layered）</td><td>按连线方向分层的 DAG / 流程图</td></tr></tbody></table><p>对应核心库 API 与参数见 <a href="./plugins.html#内置插件leaf-layout自动布局">插件系统：leaf-layout</a>。</p><h2 id="动图与媒体" tabindex="-1">动图与媒体 <a class="header-anchor" href="#动图与媒体" aria-label="Permalink to &quot;动图与媒体&quot;">​</a></h2><p>编辑器在启动时（<code>main.ts</code>）注册了核心库的媒体插件，图标可承载动态内容：</p><ul><li><strong>Lottie</strong>（<code>createLottiePlugin</code> + 注入 dotLottie 播放器）：<code>.lottie</code> / <code>.json</code> 动画，支持 Worker / Canvas2D / WebGL2 / WebGPU 多后端，可在「绑定」页签切换渲染模式。</li><li><strong>视频</strong>（<code>createVideoPlugin</code>）：mp4 / webm，浏览器原生解码逐帧贴回画布。</li><li><strong>动态 WebP / GIF</strong>（<code>createWebpPlugin</code>）：<code>ImageDecoder</code> 逐帧解码。</li><li><strong>弹力连线 / 虚线流动</strong>（<code>createElasticPlugin</code> / dashflow 样式）：连接线可开启弹簧波形或虚线流动。</li></ul><p>机制详见 <a href="./plugins.html#内置插件动图与媒体lottie-video-webp">插件系统：动图与媒体</a>。</p><h2 id="多人协作" tabindex="-1">多人协作 <a class="header-anchor" href="#多人协作" aria-label="Permalink to &quot;多人协作&quot;">​</a></h2><p>编辑器内置基于本地文档服务（<code>services/document-server.ts</code>）的实时协作：画布标签显示当前在线人数，通过 awareness 通道共享协作者的光标与选择，文档 / 资源 / 目录变更实时广播到其他客户端。协作是编辑器层的能力，核心库本身只负责渲染与数据模型。</p><h2 id="布局与运行交互配置" tabindex="-1">布局与运行交互配置 <a class="header-anchor" href="#布局与运行交互配置" aria-label="Permalink to &quot;布局与运行交互配置&quot;">​</a></h2><p>样式页签的「布局」组暴露核心库的宿主吸附与全屏适配能力（见 <a href="./elements.html#宿主吸附-host">元素系统：宿主吸附</a>）：</p><table tabindex="0"><thead><tr><th>配置项</th><th>对应核心库</th><th>说明</th></tr></thead><tbody><tr><td>宿主图元</td><td><code>node.setHost(host)</code></td><td>从元素列表拖入图形即吸附为该节点的宿主；宿主移动 / 缩放时节点跟随</td></tr><tr><td>全屏适配</td><td>样式 <code>fullscreen</code></td><td><code>fill</code> 拉伸填满视口 / <code>uniform</code> 等比完整显示，编辑模式下不生效</td></tr><tr><td>水平布局</td><td>样式 <code>layoutH</code></td><td>左 / 右 / 水平居中 / 左右拉伸 / 等比缩放</td></tr><tr><td>垂直布局</td><td>样式 <code>layoutV</code></td><td>上 / 下 / 垂直居中 / 上下拉伸 / 等比缩放</td></tr></tbody></table><p>图纸属性面板的「运行交互」组配置预览模式下的视口行为，写入图纸顶层 <code>attrs</code>（见 <a href="./serialization.html#运行配置-attrs">序列化：运行配置 attrs</a>）：</p><table tabindex="0"><thead><tr><th>配置项</th><th>对应 storage attr</th><th>说明</th></tr></thead><tbody><tr><td>适配内容</td><td><code>fitContent</code></td><td>预览打开时自动 <code>fitContent</code>，图纸内容居中适配</td></tr><tr><td>可缩放</td><td><code>zoomable</code></td><td>非编辑模式锁定 / 允许滚轮与手势缩放</td></tr><tr><td>可平移</td><td><code>pannable</code></td><td>非编辑模式锁定 / 允许拖拽平移</td></tr></tbody></table><h2 id="与核心库的关系" tabindex="-1">与核心库的关系 <a class="header-anchor" href="#与核心库的关系" aria-label="Permalink to &quot;与核心库的关系&quot;">​</a></h2><p>编辑器的每一步操作都是对核心库 API 的直接调用，可以把它当作「会说话的示例代码」：</p><table tabindex="0"><thead><tr><th>编辑器动作</th><th>核心库 API</th></tr></thead><tbody><tr><td>拖入图形 / 绘制图元</td><td><code>new Shape()</code> → <code>storage.add(shape)</code></td></tr><tr><td>修改样式面板</td><td><code>element.s({ ... })</code></td></tr><tr><td>修改绑定值</td><td><code>image.a(name, value)</code></td></tr><tr><td>保存图纸</td><td><code>storage.recalculateContentRect()</code> → <code>storage.serialize()</code></td></tr><tr><td>打开图纸</td><td><code>storage.deserialize(json)</code> → <code>view.fitContent()</code></td></tr><tr><td>撤销 / 重做</td><td><code>storage.undo()</code> / <code>storage.redo()</code></td></tr><tr><td>滤镜链编辑</td><td><code>node.filterEffects([...])</code>（FilterDescriptor 多滤镜链）</td></tr><tr><td>动画参数编辑</td><td><code>element.setAnimations([...])</code> + <code>getAnimationEffectMetas()</code></td></tr><tr><td>控制点模式编辑</td><td><code>polyline.setControlPointMode(i, mode)</code> / Connector 同组 API</td></tr><tr><td>预览</td><td>新建 <code>View</code>（只读模式）渲染同一份 JSON</td></tr></tbody></table><p>动画参数表单通过 <code>getAnimationEffect(type)?.meta</code> 读取当前动画效果的参数元信息渲染；滤镜编辑器基于核心库的 <code>filterEffects</code>（FilterDescriptor）体系，覆盖全部 35 种内置效果类型，并能把旧图纸的滤镜类配置迁移为滤镜效果；绑定值控件按 <code>valueType</code> 分发。复制 / 粘贴对应 <code>view.copyToClipboard()</code> / <code>view.pasteFromClipboard()</code>（<code>ClipboardController</code>，跨标签页用 <code>view.clipboard.getData()</code> / <code>setData()</code> 共享）；自动布局对应 <code>view.layout(options)</code>（布局插件）。</p><h2 id="本地运行" tabindex="-1">本地运行 <a class="header-anchor" href="#本地运行" aria-label="Permalink to &quot;本地运行&quot;">​</a></h2><p>编辑器是 pnpm workspace 的 <code>packages/editor</code> 包：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 安装依赖（仓库根目录）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 开发服务器</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev:editor</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 构建</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> packages/editor</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 单元测试（vitest + jsdom）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dir</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> packages/editor</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span></span></code></pre></div><p>源码结构：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>packages/editor/src</span></span>
<span class="line"><span>├── App.vue                  # 主逻辑（工具、图纸管理、属性应用）</span></span>
<span class="line"><span>├── components/              # 工具栏 / 资源面板 / 属性面板 / 各编辑器对话框</span></span>
<span class="line"><span>├── config/                  # 绑定类型、属性表单、stencil 类目清单（脚本生成）</span></span>
<span class="line"><span>└── utils/                   # svg-to-symbol、drawio/dxf/mermaid 转换等</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">注意</p><p><code>public/assets/stencils/</code> 下新增 stencil XML 库后，需重新生成类目清单：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">node</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> packages/editor/scripts/generate-stencil-manifest.mjs</span></span></code></pre></div></div><h2 id="下一步" tabindex="-1">下一步 <a class="header-anchor" href="#下一步" aria-label="Permalink to &quot;下一步&quot;">​</a></h2><ul><li><a href="./quick-start.html">快速开始</a> —— 在自己的项目里集成核心库</li><li><a href="./binding.html">图标绑定</a> —— 编辑器「绑定」页签背后的机制</li><li><a href="./serialization.html">序列化与反序列化</a> —— 编辑器保存 / 加载的 JSON 格式</li></ul>`,34)])])}const k=e(o,[["render",i]]);export{g as __pageData,k as default};
